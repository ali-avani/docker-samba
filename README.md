# Samba on docker
A docker container which run samba share with selected credentials.

## Installation
Clone the repo:

```
git clone https://github.com/ali-avani/docker-samba.git
cd docker-samba
```

Edit configuration:
```
cp .env.sample .env
vim .env
```
Run docker:
```
docker compose up -d
```
